/**
 * Default Nostr relays for metadata fetch/publish.
 * Used when the NIP-07 extension doesn't provide relay hints.
 */
export const DEFAULT_RELAYS = [
  "wss://relay.damus.io",
  "wss://relay.nostr.band",
  "wss://nos.lol",
  "wss://relay.primal.net",
];
